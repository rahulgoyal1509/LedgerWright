fastapi==0.115.0
uvicorn[standard]==0.30.6
pandas==2.2.2
pydantic==2.9.2
python-multipart==0.0.9
openpyxl==3.1.5
pdfplumber==0.11.4
rapidfuzz==3.9.7
python-dateutil==2.9.0
# Optional, only needed for the OCR fallback on scanned PDF statements:
# pytesseract==0.3.13
# pdf2image==1.17.0

# Optional, only needed for real LLM triage — without it, triage.py falls
# back to a deterministic heuristic so the pipeline still runs end-to-end:
# google-generativeai==0.8.3
